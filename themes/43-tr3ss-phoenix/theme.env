THEME_NAME="tr3ss-phoenix"
THEME_RANK="43"
THEME_BG="#1a1a1a"
THEME_FG="#fab387"
THEME_ACCENT="#ff6b35"

# >>> theme-picker osc >>>
# Sends terminal foreground/background/cursor color via OSC sequences.
# Most modern terminals honor this (iTerm2, Ghostty, kitty, alacritty,
# WezTerm, recent Terminal.app). No-op when not on a TTY.
if [ -t 1 ] && [ -z "${THEME_PICKER_NO_OSC:-}" ]; then
  printf "\033]10;#fab387\007"  # foreground
  printf "\033]11;#1a1a1a\007"  # background
  printf "\033]12;#ff6b35\007" # cursor
fi
# <<< theme-picker osc <<<
